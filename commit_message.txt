fix: タップ攻撃が動作しない問題を修正

問題:
- attackLevelが0の場合、ダメージが0になってタップ攻撃が効かない

修正内容:
- DecimalWrapper.calculateDamage()でattackLevelが0以下の場合、1として扱うように修正
- BattleSceneでattackLevelを読み込む際に、0の場合は1として扱うように修正
- デバッグログを追加して、タップイベントの動作を確認できるようにした

これにより、初期状態（attackLevel=0）でもタップ攻撃が正常に動作するようになりました。


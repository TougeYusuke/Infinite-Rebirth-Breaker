# セットアップガイド

Infinite Rebirth Breaker プロジェクトのセットアップ手順です。

## 📋 前提条件

- Node.js 18以上がインストールされていること
- npm がインストールされていること
- Git がインストールされていること（オプション）

## 🚀 セットアップ手順

### 1. 依存関係のインストール

```bash
cd "D:\my_mind_project\Infinite Rebirth Breaker"
npm install
```

### 2. 開発サーバーの起動

```bash
npm run dev
```

ブラウザで `http://localhost:3000` が自動的に開きます。

### 3. テストの実行

```bash
# テストを実行
npm test

# ウォッチモードでテスト実行（ファイル変更時に自動実行）
npm run test:watch

# テストUIを開く
npm run test:ui

# カバレッジレポートを生成
npm run test:coverage
```

### 4. ビルド

```bash
# プロダクションビルド
npm run build

# ビルド結果のプレビュー
npm run preview
```

## 🔧 トラブルシューティング

### エラー: "Cannot find module 'break_infinity.js'"

```bash
npm install break_infinity.js
```

### エラー: "Cannot find module 'phaser'"

```bash
npm install phaser
```

### TypeScriptの型エラー

```bash
# 型定義を再インストール
npm install --save-dev @types/node
```

### テストが実行されない

```bash
# Vitestを再インストール
npm install --save-dev vitest jsdom
```

## 📁 プロジェクト構造

```
Infinite Rebirth Breaker/
├── src/                    # ソースコード
│   ├── core/              # コアゲームロジック
│   ├── systems/           # ゲームシステム
│   ├── utils/             # ユーティリティ
│   ├── scenes/            # Phaserシーン
│   ├── ui/                # UIコンポーネント
│   ├── types/             # 型定義
│   └── main.ts            # エントリーポイント
├── tests/                 # テストファイル
├── public/                # 静的ファイル
├── docs/                  # ドキュメント
└── dist/                  # ビルド出力（自動生成）
```

## 🎮 開発の流れ

1. **機能実装**
   - `src/` ディレクトリにコードを追加
   - TypeScriptの型定義を適切に設定

2. **テスト作成**
   - `tests/` ディレクトリにテストを追加
   - `npm run test:watch` でテストを監視

3. **動作確認**
   - `npm run dev` で開発サーバーを起動
   - ブラウザで動作を確認

4. **ビルド**
   - `npm run build` でプロダクションビルド
   - `dist/` ディレクトリに出力される

## 📚 参考資料

- [実装ベース仕様書](../../my_mind/50_Projects/Infinite Rebirth Breaker/実装ベース仕様書.md) - 詳細な仕様（Obsidian側）
- [README.md](../README.md) - プロジェクト概要
- [Phaser 3 Documentation](https://photonstorm.github.io/phaser3-docs/)
- [break_infinity.js Documentation](https://github.com/Patashu/break_infinity.js)
- [Vite Documentation](https://vitejs.dev/)

